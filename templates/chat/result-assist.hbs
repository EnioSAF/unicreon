
<div class="unicreon-card">
  <h3>Assistance — {{name}}</h3>
  <p>Base {{capitalize carac}} / {{skillDie}} — Diff. {{baseDiff}} {{#if helpers}}(Aides: {{helpers}}){{/if}} {{#if pk}}(PK {{pk}}){{/if}}</p>
  <p>Diff finale : <b>{{finalDiff}}</b> → Jet gardé : <b>{{kept}}</b> → {{#if success}}✅ Réussite{{else}}❌ Échec{{/if}}</p>
</div>
